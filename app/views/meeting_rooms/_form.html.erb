<%= form_for(@meeting_room) do |f| %>
  <% if @meeting_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meeting_room.errors.count, "error") %> prohibited this meeting_room from being saved:</h2>

      <ul>
      <% @meeting_room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :room_name %><br>
    <%= f.text_field :room_name %>
  </div>
  <div class="field">
    <%= f.label :attendees %><br>
    <%= f.text_field :attendees %>
  </div>
  <div class="field">
    <%= f.label :booking_from %><br>
    <%= f.text_field :booking_from %>
  </div>
  <div class="field">
    <%= f.label :booking_to %><br>
    <%= f.text_field :booking_to %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
